# C05 — Legacyify Character

![Untouched C05 result](raw.png)

One user-requested action, one built-in image request, one raw returned image. No retries, selection between candidates, descendant edits, cropping or retouching.

The supplied image was assigned the overhead victory pose, wide bent-knee stance and portrait framing. The verified canonical male master was assigned identity, proportions, black clothing, illustrated rendering and blunt hand anatomy. The master's closed-mouth expression was retained because the user assigned the supplied image pose/framing authority only.

**Conditional result:** pose transfer, recognizable identity and blunt fists work. The frozen prompt incorrectly specifies a polo shirt where the master has no visible polo placket, and the output adds that feature plus a conspicuous belt buckle. This is a prompt-compilation error against the master, not solely a model fidelity miss. Fine eye, muzzle and mane construction varies, and overhead whitespace is reduced. The prompt and raw remain unchanged so the error stays visible in the record.

The small clenched fists in the source leave fine finger geometry unspecified. The master supplies that anatomy. Exact facial and mane geometry remain conditional; this new pose is one example, not a repeatability benchmark. The image was supplied by the user; its origin beyond that was not verified or classified as real photography.

- [Original returned image](raw.png)
- [Frozen prompt](prompt.txt)
- [Frozen source roles, hashes and acceptance brief](brief.json)
- [Exact tool-call arguments](tool-call.json)
- [Visual review](review.md)
- [Hash-chained live attempt ledger](../attempts.jsonl)
- [Ledger validation](validation.json)

This fresh invocation is recorded separately from the completed overnight development package. Original overnight artifacts remain unchanged. Any further revision is a new action and a new recorded image request.
