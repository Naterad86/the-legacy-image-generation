# R0-A2 Geometry-Lock Isolation — Resumption Attempt

**Status:** ABORTED — GENERATION-ROUTE FAILURE; R0-A2 NOT EVALUATED  
**Date:** 2026-08-22  
**Frozen packet:** `experiments/R0-A2_GEOMETRY_LOCK_ISOLATION_FROZEN_2026-08-22.md`

## Authority verification

The exact frozen source was retrieved directly from Google Drive before generation:

- Filename: `G1-A_HOLDOUT_OSLO_OPERA_HOUSE_BASE_SOURCE_INTERNAL_VALIDATION_2026-08-20.jpg`
- Drive ID: `1hcaY5KRnVWCbmBHb1O_jwj-oNHp9a8ZR`
- Verified SHA-256: `f2445f76358774a363ad9a4783b4b3c196f561e8f2a040b94dbcf5b19c174b75`
- Verified raster: `5520 × 3680` JPEG

The retrieved local bytes matched the frozen packet hash exactly.

## Resumed execution outcome

The frozen Laboratory instruction was retrieved and held unchanged. The authorized requirement remained exactly three independent standalone scene outputs from the untouched source, with no correction between outputs and no Evaluation before all three existed.

The current image-generation route again failed to execute the source-conditioned scene-only protocol. Six attempted generations returned multi-panel/protocol-style or unrelated scene graphics instead of a standalone Oslo Opera House re-render governed by the frozen source.

These six generated graphics are operationally invalid for R0-A2 and are excluded from experiment evidence. They were not uploaded to `03 Laboratory Evidence`, were not counted toward the three-run budget, and must not be used as authorities, references, descendants for correction, or Evaluation inputs.

No valid resumed R0-A2 scene output was produced. No frozen evaluation gate was run and no R0-A2 hypothesis classification is authorized.

## Process finding

A fresh conversation inside the ChatGPT project did not provide sufficient Laboratory context isolation. Project-level experiment history and orchestration language remained available to the generation route strongly enough that it continued to visualize the protocol rather than execute the source-conditioned scene transformation.

Therefore:

> A fresh thread inside the project is not equivalent to an isolated Laboratory execution context for this route.

## Resumption rule

Do not alter the frozen R0-A2 hypothesis, held/released boundary, source authority, instruction, or gates because of this orchestration failure.

Resume only in a genuinely isolated Laboratory execution context containing the exact frozen generation instruction, the exact Drive source, and the minimum retrieval metadata needed to verify it, without project-history injection or prior protocol/evaluation graphics.

Generate exactly three standalone scene outputs, preserve them in Drive, and hard-stop before independent Evaluation.
